{
  "installCommand": "echo done",
  "buildCommand": "cd upos && npx pnpm@9 install --no-frozen-lockfile && npx pnpm@9 --filter portal build",
  "outputDirectory": "upos/apps/portal/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
